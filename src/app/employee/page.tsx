import {StatusAuthorization} from "@/modules";

const Page = () => {
    return (
        <div>
            user employee
            <StatusAuthorization />
        </div>
    );
};

export default Page;